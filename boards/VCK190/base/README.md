# VCK190 Base Overlay

This directory contains the base overlay for the VCK190 Versal board.

The base design includes:
- AXI GPIO for LEDs (4) and buttons (4)
- PL clocks (pl0-pl3) at 100 MHz

Build the overlay with Vivado to produce `base.pdi`.
